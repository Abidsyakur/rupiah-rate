version: 2

# =============================================================================
# tests/generic/null_checks.yml (DOCUMENTATION ONLY)
# =============================================================================
# Reference guide for which columns need not_null tests
# Actual tests are defined in each model's schema.yml
#
# Reference:
# - stg_currencies: currency_id, currency_code, currency_name
# - stg_api_sources: source_id, source_name
# - stg_exchange_rates: rate_id, from_currency_id, to_currency_id, source_id, rate, rate_timestamp
# - stg_api_calls: call_id, source_id, status
# - stg_data_quality_metrics: metric_id, rate_id, check_name, check_passed
# - int_daily_exchange_rates: daily_rate_id, rate_open, rate_close
# - int_api_performance: perf_id, total_calls
# - int_quality_summary: quality_summary_id, total_rates_evaluated
# - dim_currencies: currency_id, currency_code
# - dim_api_sources: source_id, source_name
# - fct_exchange_rates: rate_id, rate, quality_flag
# - fct_daily_snapshots: snapshot_id, rate_open, rate_close, quality_flag
#
# Apply these tests in each model's schema.yml under columns section