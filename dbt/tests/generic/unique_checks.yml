version: 2

# =============================================================================
# tests/generic/unique_checks.yml (DOCUMENTATION ONLY)
# =============================================================================
# Reference guide for which columns need unique/uniqueness tests
# Actual tests are defined in each model's schema.yml
#
# Single-column unique:
# - stg_currencies: currency_id, currency_code
# - stg_api_sources: source_id, source_name
# - stg_exchange_rates: rate_id
# - stg_api_calls: call_id
# - stg_data_quality_metrics: metric_id
# - int_daily_exchange_rates: daily_rate_id
# - int_exchange_rate_statistics: daily_rate_id
# - int_api_performance: perf_id
# - int_quality_summary: quality_summary_id
# - dim_currencies: currency_id, currency_code
# - dim_api_sources: source_id, source_name
# - dim_quality_metrics: check_name
# - fct_exchange_rates: rate_id
# - fct_daily_snapshots: snapshot_id
#
# Multi-column unique (via dbt_utils.unique_combination_of_columns):
# - stg_exchange_rates: (from_currency_id, to_currency_id, source_id, rate_timestamp)
# - stg_data_quality_metrics: (rate_id, check_name)
# - int_daily_exchange_rates: (from_currency_id, to_currency_id, source_id, rate_date)
# - int_api_performance: (source_id, call_date, call_hour)
# - int_quality_summary: (currency_pair, source_id, rate_date)
# - fct_daily_snapshots: (currency_pair, source_id, rate_date)
#
# Apply these tests in each model's schema.yml under columns section